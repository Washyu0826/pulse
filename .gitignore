# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Env
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Docker volumes
postgres_data/

# 企業/校園 TLS 攔截用的根憑證（機器相關、勿入庫）。放 workers/ca-certs/ 由 image 自動裝入。
*.pem
workers/ca-certs/*.crt

# Logs
*.log
logs/

# ML
*.pt
*.bin
mlruns/
.cache/
/models/

# Generated datasets (silver labels regenerable from DB; large).
# gold sets are small + valuable — force-add when they exist: git add -f data/gold/gold.jsonl
data/

# Claude Code local agent data (large, machine-local)
.claude/

# 與本專案無關（碩論模板，非 Pulse）
碩論模板.pdf

# OS
Thumbs.db
scripts/daily_refresh.log

# 訓練 / 機器產生的 log 工件（機器專屬，勿入庫）
logs_theme_train.txt
logs/
*.log.bak

# 本機 TLS 攔截（Avast / 企業 MITM proxy）根憑證 —— 機器專屬，Docker build 期選配掛入，切勿入庫。
# 只忽略憑證檔，保留 certs/ 目錄（.gitkeep 入庫）讓 Dockerfile 的 COPY certs/ 在乾淨 checkout 也成立。
*/certs/*.crt
*/certs/*.pem
certs/*.crt
certs/*.pem
