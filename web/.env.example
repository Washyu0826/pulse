# Pulse web 環境變數範本 —— 複製成 .env.local 後依環境調整。
# 注意：程式碼實際讀的是 NEXT_PUBLIC_API_URL（lib/utils.ts）與 NEXT_PUBLIC_SITE_URL（lib/site.ts）。

# 後端 API 位址（本機 API 跑在 8010，見 MEMORY: Local Run Setup）
NEXT_PUBLIC_API_URL=http://localhost:8010

# 站台對外網址（SEO / OG / sitemap 用；本機開發可留 localhost）
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# （選填）Server Component 走容器內部網路時的 API 位址，未設則沿用 NEXT_PUBLIC_API_URL
# API_URL_INTERNAL=http://api:8010
